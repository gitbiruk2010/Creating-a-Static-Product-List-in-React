// Importing the React library to enable JSX syntax and component functionality
import React from 'react';

// Defining the Contact component as a functional component
const Contact = () => {
  // Returning JSX for rendering the Contact Page content
  return (
    <div>
      Contact Page
      </div>
  );
};

// Exporting the Contact component to make it available for use in other parts of the application
export default Contact;
